ActiveAdmin.register Student do
form(:html => { :multipart => true }) do |f|
f.inputs "Student" do
      f.input :fname
      f.input :lname
      f.input :address
      f.input :phone
      f.input :dob
      f.input :avatar, :as => :file
    end
    f.buttons
  end

end
